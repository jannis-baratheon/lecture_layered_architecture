pluginManagement {
    plugins { spec ->
        spec.id('org.springframework.boot').version('2.6.7')
        spec.id('io.spring.dependency-management').version('1.0.11.RELEASE')
    }
}

rootProject.name = 'demo'

include 'app'
include 'persistence'
